const top_down_a = document.getElementById("top_down_a");

top_down_a.addEventListener("click", err => {
    // document.documentElement.style.setProperty("--start", `${Math.floor(Math.random() * 360)}deg)`);
});

